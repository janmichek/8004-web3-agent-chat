// Oxlint configuration. Run with `npm run lint`
// (see package.json: `oxlint --type-aware --type-check --deny-warnings`).
{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "ignorePatterns": ["dist/", "frontend/dist/", "agents/"],
  "rules": {}
}
